import React, { Fragment } from "react"
import { Styled } from "theme-ui"

/**
 * Change the content to add your own bio
 */

export default () => (
  <Fragment>
    <Styled.a href="https://harryk.dev/">Harry K.</Styled.a>
    {` `}
    <br />
	Ex + Rs + Js + C/C++ 
  </Fragment>
)
